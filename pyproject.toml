[project]
name = "migrator"
version = "0.1.0"
description = "Migrate From One Source Control To Another"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]
